- @produits.each do |produit|
  -# Produit
  .list-group-item
    .col-sm-2.list-text-group
      = produit.nom
    .col-sm-1.list-text-dispo
      = produit.disponibilite
    .col-sm-3{ :style => "padding-top:3%;" }
      = produit.description
    .col-sm-2
      = image_tag produit.product_image, :class =>'list-product-img'
    .col-sm-2{ :style => "padding-top:3%;" }
      = link_to 'Show', produit, class:"btn btn-info list-group-a"
    -if(produit.disponibilite > 0)
      .col-sm-2.list-product-rent{ :style => "padding-top:3%;" }
        -if (current_user)
          = link_to 'Louer', new_user_produit_location_url(user_id: current_user, produit_id: produit.id), class:"btn btn-info list-group-a"
        -else
          = link_to 'Louer', new_user_session_url, class:"btn btn-info list-group-a"
    -else
      .col-sm-2.list-text-group
        .list-not-available Not available
